
import React, { FC, useEffect, useRef, useState } from "react";
import { View, Text, TouchableOpacity } from "react-native";
const HomeScreen: FC<{}> = (porps) => {

    <View style={{ flex: 1, backgroundColor: 'red' }}>
    </View>
  )
}
export default HomeScreen